apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name:  pvc-postgres
  labels:
    app: postgres
spec:
  storageClassName: localnfs  # change if needed
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
