apiVersion: v1
kind: ReplicationController
metadata:
  name: hellochristof-rc
spec:
  replicas: 3
  selector:
    app: hellochristofapp
  template:
    metadata:
      name: soaktestrc
      labels:
        app: soaktestrc
    spec:
      containers:
      - name: soaktestrc
        image: nickchase/soaktest
        ports:
        - containerPort: 80
