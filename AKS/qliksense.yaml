# just a starting point, no identitiy provider specified

engine:
  acceptEULA: "yes"
  
global:
  persistence:
     storageClass: "azurefile"

devMode:
  enabled: true  ## uses built-in Mongo-DB, not for production use
 
